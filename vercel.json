{
    "builds": [
      {
        "src": "next.config.js",
        "use": "@vercel/next",
        "config": {
          "installCommand": "npm install --platform=linux --arch=x64 sharp"
        }
      }
    ]
  }